import React from 'react';
import Section from "@/components/section/Section";

const UserSettings = () => {
    return (
        <div className={'page'}>
            <Section>
                User settings
            </Section>
        </div>
    );
};

export default UserSettings;